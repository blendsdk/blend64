/**
 * Dummy test for playground package
 * Tests basic functionality and imports to ensure package setup is working
 */

import { describe, it, expect } from 'vitest';

describe('Playground Package', () => {
  it('sanity',  () => {
    expect(true).toBe(true);
  });
});

/** Memory layout definition */
