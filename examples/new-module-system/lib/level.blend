module game.level

// This module is in a different directory (lib/) but still found by module name
// Demonstrates location independence

import setBackgroundColor from target.video

// Level data
var currentLevel: byte = 1

export function initLevel(): void
    setBackgroundColor(6)  // Blue background for level 1
end function

export function renderLevel(): void
    // Render level graphics
end function

export function getCurrentLevel(): byte
    return currentLevel
end function
